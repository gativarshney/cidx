# Benchmark results

No results are published yet.

When real, budget-capped model runs happen, each lands here as
`<label>/raw.jsonl` (every model call and tool call, one JSON line each) plus
`<label>/league.md` (the generated table). Every published number is
recomputable from the raw log alone — see
[../methodology.md](../methodology.md) for the rules, including the
dev/holdout split and the honesty requirements.

Development and CI runs use the recorded-response stub model and are not
published as results.
